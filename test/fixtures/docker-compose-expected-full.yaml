---
services:
  db:
    image: postgres:15
    volumes: [pgdata:/var/lib/postgresql/data]
  web:
    depends_on: [db]
    environment: [NGINX_HOST=example.com, NGINX_PORT=80]
    image: nginx:alpine
    ports: [80:80]

version: '3.8'

volumes:
  pgdata: null
